import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Gram Setu — Explainable Panchayat Priority Engine & Knapsack Optimizer",
  description: "AI-powered rural resource allocation engine utilizing 0/1 Knapsack optimization and transparent multi-criteria decision analysis (MCDA) for Indian Gram Panchayats.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased dark`}
    >
      <body className="min-h-full flex flex-col bg-[#0b0f19] text-gray-100">{children}</body>
    </html>
  );
}
